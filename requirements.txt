# Core dependencies for batch-invariant-ops
# For GPU (CUDA) support, install PyTorch separately first:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# PyTorch (install with CUDA via index-url above, not from this file)
# torch>=2.0.0

# Triton for GPU kernels
triton>=2.0.0

# Optional: vLLM testing dependencies
# Uncomment if you want to run deterministic_vllm_inference.py
# httpx>=0.24.0
# vllm>=0.3.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
# isort>=5.0.0
# flake8>=4.0.0
